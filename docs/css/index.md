---
title: Css
layout: doc
sidebar: true
aside: true
---
# CSS 

## 📐 第一部分：基石 —— 重新理解 CSS（基础篇）

**📖 第1章：CSS 的三种存在形态与层叠规则**
- 🧬 1.1 不仅仅是样式表：浏览器默认样式、用户样式、作者样式
- ⚖️ 1.2 核心世界观：层叠（Cascade）的权重算法与 `!important` 的禁区
- 🎯 1.3 来源顺序、特异性（Specificity）计算题实战
- 🛠️ 1.4 现代调试技巧：Chrome DevTools 中查看层叠层与特异性

**🔎 第2章：选择器的艺术与性能底线**
- 📇 2.1 从通配符到结构化伪类：常用选择器索引
- 🔗 2.2 容易被忽略的组合器：`>` 、`+` 、`~` 的实际应用场景
- 🤖 2.3 伪类 vs 伪元素：`::before` 实现装饰性 UI 与 `:has()` 实现父级联动
- 🚀 2.4 浏览器从右向左解析：为什么极深嵌套选择器是性能隐患

**📦 第3章：盒模型 —— 一切布局的几何基础**
- 🧩 3.1 内容区、内边距、边框、外边距的数学模型
- 💡 3.2 **关键转折点**：`box-sizing: border-box` 为什么是现代重置标配
- 🖼️ 3.3 可视化调试：通过轮廓线（Outline）与背景延伸理解盒子边界
- 💥 3.4 外边距塌陷（Margin Collapse）：发生条件与 BFC 解决方案预览

**🌊 第4章：文档流与格式化上下文（BFC / IFC）**
- 📄 4.1 标准流、浮动流、定位流的视觉差异
- 🧱 4.2 块级格式化上下文（BFC）的触发方法与内部特性（清除浮动、隔离边距）
- 📝 4.3 行内格式化上下文（IFC）与 `vertical-align` 的那些坑
- ☯️ 4.4 一种哲学：文档流是一切布局错乱的最终解药



## 🏗️ 第二部分：构建界面 —— 现代布局体系（核心篇）

**🤸 第5章：Flexbox —— 一维弹性布局完全指南**
- 📏 5.1 主轴与交叉轴：基础轴线概念的彻底可视化
- ⚙️ 5.2 容器属性详解：`justify-content`、`align-items` 与 `gap` 的黄金组合
- 🎈 5.3 项目属性实战：`flex-grow` 挤压算法与 `flex-shrink` 的收缩抵抗
- 🏛️ 5.4 经典案例：圣杯布局、等分布局、粘性页脚、自适应卡片列表

**🗂️ 第6章：Grid —— 二维网格布局终极方案**
- 📊 6.1 显式网格 vs 隐式网格：`grid-template-areas` 可视化命名区域
- 🪄 6.2 神级函数：`repeat()`、`minmax()`、`fit-content()` 实现完美自适应
- 🎯 6.3 对齐与间隙：Grid 中的对齐属性与 Flexbox 的细微差别
- 🧱 6.4 实践模式：Holy Grail 响应式布局、瀑布流（Masonry）的 Grid 模拟法

**🗺️ 第7章：定位与堆叠 —— Z 轴上的世界**
- 📌 7.1 五种 `position` 值的行为全解（Static、Relative、Absolute、Fixed、Sticky）
- 🐞 7.2 粘性定位（Sticky）失效诊断清单（祖先 `overflow` 的影响）
- 🎚️ 7.3 层叠上下文（Stacking Context）：`z-index` 失效的根本原因图解
- 🧭 7.4 总结：什么时候该用定位，什么时候该用 Grid？



## 🎨 第三部分：视觉与交互 —— 让页面“活”起来（表现篇）

**🌈 第8章：颜色革命与现代化背景**
- 🎛️ 8.1 告别十六进制：OKLCH / LCH 颜色空间与 P3 广色域支持
- 🎨 8.2 CSS 变量（自定义属性）驱动的动态主题切换方案
- 🖌️ 8.3 多重背景、渐变与混合模式（`mix-blend-mode`）的视觉创意
- 🥂 8.4 `backdrop-filter`：毛玻璃效果与现代弹窗设计

**🌀 第9章：变换与过渡 —— 流畅的 UI 反馈**
- 🔄 9.1 `transform` 属性全解析：平移、旋转、缩放、倾斜与矩阵
- ⚡ 9.2 渲染性能优化：为什么只用 `transform` 和 `opacity` 做动画？
- 📈 9.3 `transition` 与贝塞尔曲线：模仿自然物理运动的缓动函数
- 🧠 9.4 开发技巧：利用 `will-change` 提前告知浏览器准备合成层

**🎬 第10章：关键帧动画与 Houdini 展望**
- 🎞️ 10.1 `@keyframes` 语法结构与动画事件监听（JS 联动）
- 📜 10.2 复杂交互动画：`animation-timeline: view()` 实现滚动驱动动画
- 🧪 10.3 跳出限制：CSS Houdini Paint API 绘制自定义图形简介



## 📱 第四部分：应对多端 —— 响应式与工程化（进阶篇）

**📲 第11章：响应式设计的现代策略**
- 🥇 11.1 移动优先 vs 桌面优先：媒体查询断点的设定逻辑
- 📦 11.2 **告别硬编码**：容器查询（`@container`）实现组件级自适应
- 📏 11.3 视口单位新成员：`svh`、`lvh`、`dvh` 解决移动端高度视口问题
- ✒️ 11.4 流体排版：`clamp()` 函数实现字体随屏幕平滑缩放

**🏛️ 第12章：CSS 架构方法论与预处理**
- 📂 12.1 从混乱到秩序：BEM 命名规范与 OOCSS 原则落地
- 🛠️ 12.2 SCSS / Less 核心功能复用：Mixin、继承与循环生成栅格系统
- 🪹 12.3 现代原生方案：CSS Nesting 嵌套语法与 PostCSS 生态
- 🌲 12.4 性能预算：CSS 文件体积分析与 PurgeCSS 摇树优化

**🧬 第13章：革命性特性：层叠层（`@layer`）与作用域（`@scope`）**
- 🍰 13.1 为什么需要 `@layer`？解决第三方库样式覆盖的世纪难题
- 🧩 13.2 定义图层顺序：`reset`、`framework`、`theme`、`components` 分层实践
- 🔒 13.3 `@scope` 的隔离魔法：编写真正独立的组件样式而不污染全局



## 🧠 第五部分：工程化思维与技巧（专家篇）

**🖥️ 第14章：深入浏览器渲染管线（Render Pipeline）**
- 🎭 14.1 重绘（Repaint）vs 回流（Reflow）vs 合成（Composite）
- 🔬 14.2 如何利用 DevTools Performance 面板定位样式瓶颈
- 🚄 14.3 `content-visibility: auto` 与虚拟列表的 CSS 实现

**🧰 第15章：代码片段库 —— 手写常见 UI 模式**
- 💬 15.1 只用 CSS 实现的 Tooltip、Dropdown 菜单（`:focus-within` 技巧）
- 🦴 15.2 骨架屏（Skeleton Screen）的纯样式实现
- ✅ 15.3 自定义单/多选框、Switch 开关（`:checked` + 兄弟选择器技巧）

**🚀 第16章：CSS 的未来与前端人员的成长建议**
- 🔮 16.1 值得关注的草案：Masonry 布局、`if()` 条件规则、状态查询
- 🧑‍🎨 16.2 从写样式到设计系统：Token 化设计与 Figma 对接
- 🎮 16.3 保持手感：参与 CSS Battle 与 阅读 W3C CSS 规范注释


